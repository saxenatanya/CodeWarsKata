function shifter(s){
  // your code here
  let letters =["H", "I", "N", "O", "S", "X", "Z", "M" ,"W"];
  let sp =s.split(' ').map((val) => val.map(v1) => v1.contains(letters) ? count++ : null).join('');
  console.log(sp);
}
